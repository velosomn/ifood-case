# Core
pyspark==4.2.0
pandas==3.0.3
numpy==2.4.6
pyarrow==25.0.0

# Modeling
scikit-learn==1.9.0
xgboost==3.3.0

# Viz / notebooks
matplotlib==3.11.1
seaborn==0.13.2
jupyter
nbformat
nbconvert
ipykernel

# Presentation (optional, para gerar o .pptx)
python-pptx==1.0.2

# NOTE: PySpark requires a Java runtime (JDK 8/11/17). Recomendado:
#   conda install -c conda-forge openjdk=17
# NOTE: No Databricks, a sessão Spark é provida pela plataforma
#   (spark.sparkContext.getOrCreate()) — a versão de pyspark acima só
#   importa para execução local.
